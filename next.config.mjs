/** @type {import("next").NextConfig} */
const nextConfig = {
  output: "export", // Add output: "export" based on successful build log
  trailingSlash: false, // Keep trailingSlash: false as it might still be needed
};

export default nextConfig;

